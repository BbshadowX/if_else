const param1A = input1
const param1B = input2
const param2A = input3
const param2B = input4
// define 4 variables param1A, param1B, param2A, and param2B
if (param1A == param1B || param2A == param2B) {
    console.log (true)
    // if atlease one of the pair is truthy print true
    } else {
    console.log (false)
    // if nne of the pair is truthy print false
    }